---
layout: default
title: Just Dwelling Archive
---

<style>
  .page-wrap { max-width: 960px; margin: 2rem auto; padding: 0 1rem; }
  .hero { margin-bottom: 1.5rem; }
  .hero h1 { margin: 0 0 .25rem; font-size: clamp(1.6rem, 3vw, 2rem); }
  .hero p { color: #666; margin: 0; }
  .grid { display: grid; grid-template-columns: repeat(auto-fill,minmax(260px,1fr)); gap: 12px; }
  .card { border: 1px solid #e6e6e6; border-radius: 12px; padding: 14px 16px; background: #fff; }
  .title a { text-decoration: none; font-weight: 600; }
  .title a:hover { text-decoration: underline; }
  .meta { color:#666; font-size:.9rem; margin-top:6px; }
  .btn { display:inline-block; margin-top:8px; padding:6px 10px; border:1px solid #ddd; border-radius:8px; text-decoration:none; font-size:.9rem; }
  .btn:hover { background:#f6f6f6; }
</style>

<div class="page-wrap">
  <div class="hero">
    <h1>Just Dwelling Archive</h1>
    <p>Publications, reports, and other outputs — all in one place.</p>
  </div>

  {% assign all = site.static_files | where_exp: "f", "f.path contains '/pdfs/'" %}
  {% assign pdfs = all | where: "extname", ".pdf" | sort_natural: "name" %}

  <div class="grid">
    {% for f in pdfs %}
      <div class="card">
        <div class="title">
          <a href="{{ f.path | relative_url }}" target="_blank" rel="noopener">
            {{ f.name | remove: '.pdf' | replace: '-', ' ' }}
          </a>
        </div>
        <div class="meta">PDF</div>
        <a class="btn" href="{{ f.path | relative_url }}" target="_blank" rel="noopener">Download</a>
      </div>
    {% endfor %}
  </div>
</div>
